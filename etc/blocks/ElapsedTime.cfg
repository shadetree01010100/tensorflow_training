{
    "exclude": true,
    "fields": [
        {
            "formula": "{{ (datetime.datetime.now() - $timestamp).total_seconds() }}",
            "title": "elapsed_time"
        },
        {
            "formula": "{{ $predictions }}",
            "title": "predictions"
        }
    ],
    "log_level": "NOTSET",
    "name": "ElapsedTime",
    "type": "Modifier",
    "version": "1.0.0"
}