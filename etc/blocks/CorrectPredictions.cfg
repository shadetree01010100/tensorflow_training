{
    "exclude": true,
    "fields": [
        {
            "formula": "{{ __import__('numpy').mean(($labels == $prediction.round()).all(axis=-1)) }}",
            "title": "percent_correct"
        }
    ],
    "log_level": "NOTSET",
    "name": "CorrectPredictions",
    "type": "Modifier",
    "version": "1.0.0"
}